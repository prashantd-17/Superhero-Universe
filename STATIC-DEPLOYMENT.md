# Static + SEO build

This package is the working non-SSR Angular project with the latest SEO and Playwright changes integrated.

Render Static Site:
- Build Command: `npm install && npm run build`
- Publish Directory: `dist/superhero-universe/browser`
- Rewrite: `/*` -> `/index.html`

SSR files/dependencies were intentionally removed.
